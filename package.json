{
  "name": "josu",
  "version": "1.0.0",
  "main": "dist/bot/index.js",
  "repository": "https://github.com/duelist/josu",
  "author": "Ian Benedict <ianbenedict@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean-build-dir && yarn create-links && tsc",
    "build-watch": "yarn clean-build-dir && yarn create-links && tsc -w",
    "clean-build-dir": "rm -rf dist",
    "create-links": "mkdir -p dist/node_modules && ln -s .. dist/node_modules/josu",
    "lint": "eslint src",
    "start-local": "node dist/bot/index.js"
  },
  "dependencies": {
    "@slack/client": "^3.10.0",
    "github": "^9.2.0"
  },
  "devDependencies": {
    "@types/node": "^8.0.16",
    "eslint": "^4.3.0",
    "jest": "^20.0.4"
  }
}
